{
  "name": "@sse-pdf-viewer/core",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn workspace sse-react-pdf build",
    "dev": "yarn workspace sse-react-pdf watch & yarn workspace test dev",
    "lint": "yarn workspaces foreach --all run lint",
    "postinstall": "husky install",
    "prettier": "yarn workspaces foreach --all run prettier",
    "test": "yarn workspaces foreach --all run test",
    "tsc": "yarn workspaces foreach --all run tsc",
    "unit": "yarn workspaces foreach --all run unit"
  },
  "resolutions": {
    "eslint-plugin-import": "npm:eslint-plugin-i@^2.28.0"
  },
  "dependencies": {
    "pdfjs-dist": "^4.0.379",
    "tiny-invariant": "^1.3.1"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  }
}
